<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ravi.examassist">

    <application
        android:name=".core.ExamAssistApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExamAssist">
        <activity
            android:name=".views.dashboard.DashboardActivity"
            android:exported="true"
            >

        </activity>
        <activity
            android:name=".core.BaseActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.ravi.examassist.views.OnBoarding.OnBoardingActivity"
            android:exported="true"
            android:theme="@style/Theme.ExamAssist.NoActionBar">

        </activity>

    </application>

</manifest>